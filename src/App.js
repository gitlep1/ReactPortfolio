import { route, Router } from "react-router-dom";

import Navbar from "./components/navbar/navbar";

const App = () => {
  return (
    <section>
      <Navbar />
    </section>
  );
};

export default App;
